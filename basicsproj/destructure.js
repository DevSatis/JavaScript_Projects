const user = ['Satish', 2, 'Vanshika']
// //console.log(name[1]);
// var role = user[2];
// var name = user[0];
// console.log(role)

const myUser = {
    name: 'Vanshika',
    couseCount : 3,
    role : 'admin'
};
console.log(myUser.couseCount);
const {name, myCourseCount, role} = myUser;
